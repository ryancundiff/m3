#include <string>

namespace str {
  std::string make_bold (std::string str);
  std::string make_gray (std::string str);
  std::string make_red (std::string str);
  std::string make_green (std::string str);
}